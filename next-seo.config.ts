const siteName = 'Kevlar'
const title = `${siteName} - Make Your Wallet Trustless`
const description = 'Kevlar makes Metamask, or any RPC-based wallet, completely trustless.'
const url = 'https://kevlar.sh'

export default {
  title,
  description,
  openGraph: {
    type: 'website',
    locale: 'en_US',
    url,
    siteName,
    title,
    description,
  },
}
